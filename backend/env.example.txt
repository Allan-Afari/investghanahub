# Database Configuration
DATABASE_URL="postgresql://postgres:password@localhost:5432/investghanahub?schema=public"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="24h"

# Server Configuration
PORT=5000
NODE_ENV="development"

# Encryption Key for Ghana Card Numbers (must be 32 characters)
ENCRYPTION_KEY="your-32-character-encryption-key!"

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Frontend URL (for CORS)
FRONTEND_URL="http://localhost:5173"

# ===========================================
# EMAIL CONFIGURATION (Nodemailer)
# ===========================================
# For Gmail: Enable "Less secure app access" or use App Password
# For production: Use services like SendGrid, Mailgun, AWS SES
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# ===========================================
# PAYSTACK CONFIGURATION (Payments)
# ===========================================
# Get keys from: https://dashboard.paystack.com/#/settings/developer
PAYSTACK_SECRET_KEY="sk_test_xxxxxxxxxxxxxxxxxxxxxxxx"
PAYSTACK_PUBLIC_KEY="pk_test_xxxxxxxxxxxxxxxxxxxxxxxx"

# ===========================================
# CLOUDINARY CONFIGURATION (File Uploads)
# ===========================================
# Get credentials from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# ===========================================
# SMS CONFIGURATION (Optional - for OTP)
# ===========================================
# Hubtel SMS API (Ghana)
HUBTEL_CLIENT_ID="your-client-id"
HUBTEL_CLIENT_SECRET="your-client-secret"
HUBTEL_SENDER_ID="InvestGH"

# ===========================================
# PROFIT DISTRIBUTION SCHEDULER
# ===========================================
# Enable automatic profit distribution (checks matured investments)
ENABLE_PROFIT_SCHEDULER="false"  # Set to "true" in production
# Interval in milliseconds (default: 3600000 = 1 hour)
PROFIT_CHECK_INTERVAL_MS=3600000

# ===========================================
# IMPORTANT NOTES
# ===========================================
# 1. Rename this file to .env
# 2. Replace all placeholder values with real credentials
# 3. Never commit the .env file to version control
# 4. For production, use environment variables from your hosting provider
# 5. Set ENABLE_PROFIT_SCHEDULER=true in production for automatic profit distribution

